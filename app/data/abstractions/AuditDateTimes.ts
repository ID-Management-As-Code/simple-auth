export interface AuditDateTimes {
    /**
     * The ISO-formatted date-time the document was created at.
     */
    createdAt: string;

    /**
     * The ISO-formatted date-time the document was updated at.
     */
    updatedAt: string;
}
